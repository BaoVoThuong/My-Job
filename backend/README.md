# MyJob Backend

Backend API for MyJob Online Recruitment Platform

## Setup

1. Install dependencies: `npm install`
2. Copy `.env.example` to `.env` and configure
3. Run development server: `npm run dev`
